{
  "name": "@txkit/example-arbitrum-london",
  "version": "0.0.0",
  "private": true,
  "description": "txKit Arbitrum London Buildathon scaffold. Shipped: AgentPolicyGate Solidity contract with EIP-712 binding + 15 Foundry tests, chain configs for Arbitrum Sepolia + Robinhood Chain testnet, decoder data under decoder-data/. Pending: Pendle yield swap demo (scenario A), x402 RWA agent on Robinhood Chain (scenario C), Next.js app (run `pnpm create next-app@15 .` to scaffold).",
  "scripts": {
    "build:contracts": "cd contracts && forge build",
    "test:contracts": "cd contracts && forge test"
  }
}
